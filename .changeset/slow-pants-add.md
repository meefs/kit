---
'@solana/errors': minor
---

Add the `SOLANA_ERROR__WALLET__ACCOUNT_NOT_AVAILABLE` error code, thrown when a selected account is not available in the connected wallet. Its context carries the requested `address` and the `walletName`.
