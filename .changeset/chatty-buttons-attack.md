---
'@solana/instructions': major
---

BREAKING CHANGE: Removes the following deprecated types: `IAccountMeta`, `IAccountLookupMeta`, `IInstruction`, `IInstructionWithAccounts` and `IInstructionWithData`.
